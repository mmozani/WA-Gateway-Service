{
  "name": "wa-api",
  "version": "2.0.0",
  "description": "NeoZone WhatsApp OTP Gateway",
  "main": "server.js",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "link": "node scripts/link-session.js",
    "check": "node --check server.js && node --check config.js && node --check middleware/auth.js && node --check services/dedupe.js && node --check services/whatsapp.js && node --check templates/otp.js && node --check scripts/link-session.js"
  },
  "keywords": [
    "neozone",
    "whatsapp",
    "otp"
  ],
  "author": "Parsian Digital Raya",
  "license": "UNLICENSED",
  "dependencies": {
    "dotenv": "17.4.2",
    "express": "5.2.1",
    "express-rate-limit": "8.7.0",
    "qrcode-terminal": "0.12.0",
    "whatsapp-web.js": "1.34.7"
  }
}
